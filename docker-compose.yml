services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - ./data:/app/backend/data   # SQLite persists across rebuilds
    restart: unless-stopped
